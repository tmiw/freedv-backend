diff --git a/src/vec_neon.h b/src/vec_neon.h
index 31b736c..ea2b0ba 100644
--- a/src/vec_neon.h
+++ b/src/vec_neon.h
@@ -299,63 +299,58 @@ static inline void sgemv(float *out, const float *weights, int rows, int cols, i
    }
 }
 
-/* Temporarily use unoptimized version */
+/* NEON: same 8-row-block / 4-column-unroll layout as the scalar version
+** this replaces, but the 8 output rows per block are held in two
+** float32x4_t accumulators (y0_3/y4_7) -- the same split sgemv8x1()
+** above uses -- instead of 8 scalar variables. Each of the 4 unrolled
+** sparse columns becomes one broadcast-load + two 4-wide FMAs instead
+** of 8 separate scalar multiply-adds. Mirrors vec_avx.h's
+** sparse_sgemv8x4, which does the equivalent 8-wide FMA in one AVX
+** register; NEON needs two 4-wide registers to cover the same 8 rows. */
 static inline void sparse_sgemv8x4(float *out, const float *w, const int *idx, int rows, const float *x)
 {
    int i, j;
-   RNN_CLEAR(out, rows);
    for (i=0;i<rows;i+=8)
    {
+      float *y = &out[i];
+      float32x4_t y0_3 = vdupq_n_f32(0);
+      float32x4_t y4_7 = vdupq_n_f32(0);
       int cols;
       cols = *idx++;
       for (j=0;j<cols;j++)
       {
          int pos;
-         float * restrict y;
-         float xj0, xj1, xj2, xj3;
+         float32x4_t xj, w0_3, w4_7;
          pos = (*idx++);
-         xj0 = x[pos+0];
-         xj1 = x[pos+1];
-         xj2 = x[pos+2];
-         xj3 = x[pos+3];
-         y = &out[i];
-         y[0] += w[0]*xj0;
-         y[1] += w[1]*xj0;
-         y[2] += w[2]*xj0;
-         y[3] += w[3]*xj0;
-         y[4] += w[4]*xj0;
-         y[5] += w[5]*xj0;
-         y[6] += w[6]*xj0;
-         y[7] += w[7]*xj0;
-
-         y[0] += w[8]*xj1;
-         y[1] += w[9]*xj1;
-         y[2] += w[10]*xj1;
-         y[3] += w[11]*xj1;
-         y[4] += w[12]*xj1;
-         y[5] += w[13]*xj1;
-         y[6] += w[14]*xj1;
-         y[7] += w[15]*xj1;
-
-         y[0] += w[16]*xj2;
-         y[1] += w[17]*xj2;
-         y[2] += w[18]*xj2;
-         y[3] += w[19]*xj2;
-         y[4] += w[20]*xj2;
-         y[5] += w[21]*xj2;
-         y[6] += w[22]*xj2;
-         y[7] += w[23]*xj2;
-
-         y[0] += w[24]*xj3;
-         y[1] += w[25]*xj3;
-         y[2] += w[26]*xj3;
-         y[3] += w[27]*xj3;
-         y[4] += w[28]*xj3;
-         y[5] += w[29]*xj3;
-         y[6] += w[30]*xj3;
-         y[7] += w[31]*xj3;
+
+         xj = vld1q_dup_f32(&x[pos+0]);
+         w0_3 = vld1q_f32(&w[0]);
+         w4_7 = vld1q_f32(&w[4]);
+         y0_3 = vmlaq_f32(y0_3, w0_3, xj);
+         y4_7 = vmlaq_f32(y4_7, w4_7, xj);
+
+         xj = vld1q_dup_f32(&x[pos+1]);
+         w0_3 = vld1q_f32(&w[8]);
+         w4_7 = vld1q_f32(&w[12]);
+         y0_3 = vmlaq_f32(y0_3, w0_3, xj);
+         y4_7 = vmlaq_f32(y4_7, w4_7, xj);
+
+         xj = vld1q_dup_f32(&x[pos+2]);
+         w0_3 = vld1q_f32(&w[16]);
+         w4_7 = vld1q_f32(&w[20]);
+         y0_3 = vmlaq_f32(y0_3, w0_3, xj);
+         y4_7 = vmlaq_f32(y4_7, w4_7, xj);
+
+         xj = vld1q_dup_f32(&x[pos+3]);
+         w0_3 = vld1q_f32(&w[24]);
+         w4_7 = vld1q_f32(&w[28]);
+         y0_3 = vmlaq_f32(y0_3, w0_3, xj);
+         y4_7 = vmlaq_f32(y4_7, w4_7, xj);
+
          w += 32;
       }
+      vst1q_f32(&y[0], y0_3);
+      vst1q_f32(&y[4], y4_7);
    }
 }
 
